packages:
  - "apps/*"
  - "packages/*"

# Packages allowed to run install scripts (pnpm v10+ requires explicit approval).
# Keep this list minimal — every entry is a supply-chain risk acceptance.
onlyBuiltDependencies:
  - "@biomejs/biome"
  - "@clerk/shared"
  - esbuild
  - sharp
